# GitHub history archive

Exported 2026-08-30 from the accidentally-transferred repository's GitHub
attachments (these belong to the repo object, not git refs, so they did not
come along with the branch restore):

- prs/ — all 34 pull requests: metadata, bodies, comments, reviews, commits, changed files
- deployments/ — all 30 deployment records + their statuses (Vercel et al.)
- environments.json — the 7 environments' configuration (secrets are never exportable)
