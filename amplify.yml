version: 1
applications:
  - appRoot: apps/nextjs
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
        build:
          commands:
            - pnpm install
            - echo "Current working directory:"
            - pwd
            - env >> .env.production
            - pnpm exec turbo run build --filter=@court-base/nextjs
            - pnpm -w run db:migrate:deploy # -w because appRoot is not the root of the monorepo
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - apps/nextjs/.next/cache/**/*
          - node_modules/**/*
    buildPath: apps/nextjs